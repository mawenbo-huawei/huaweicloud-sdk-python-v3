[tox]
envlist = py37, py38, py39, py310, py311, py312, py313

[testenv]
deps =
    -r requirements.txt
    -r test-requirements.txt
commands =
    pytest tests/ {env:PYTEST_EXTRA_ARGS:-v}

[pytest]
testpaths = tests
